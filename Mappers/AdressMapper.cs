using AMLWatch.Models;
using AMLWatch.DTOs;

namespace AMLWatch.Mappers;

public static class AdressMapper
{
    // public static AdressDTO ToDto(this Client client)
    //     => new AdressDTO(Street = client.Street, City = client.City, ZipCode = client.ZipCode, Country = client.Country);


    // public static Client ToEntity(this AdressDTO dto)
    //     => new Adress(dto.Street, dto.City, dto.ZipCode, dto.Country)
    //     {
    //         Id = dto.Id
    //     };

}

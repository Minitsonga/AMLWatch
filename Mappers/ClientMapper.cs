using AMLWatch.Models;
using AMLWatch.DTOs;

namespace AMLWatch.Mappers;

public static class ClientMapper
{
    //     public static ClientDTO ToDto(this Client client)
    //         => new(client.Id, client.Username, client.Email, client.Adress);

    //     public static Client ToEntity(this ClientDTO dto)
    //         => new Client(dto.Username, dto.FirstName, dto.LastName, dto.Email, dto.PasswordHash)
    //         {
    //             Id = dto.Id,
    //             Adress = dto.adress
    //         };
    
}
